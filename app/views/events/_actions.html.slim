= link_to t('action.edit'), edit_event_path(event), :class => "btn btn-warning"
= link_to t('action.delete'), event_path(event), method: :delete, data: { confirm: I18n.t("notice.are_you_sure") }, :class => "btn btn-danger"